# 采集与汇总功能说明

## 功能概述

新增了三个核心功能：
1. **采集控制** - 可以开始和停止历史记录的采集
2. **汇总提示词配置** - 自定义历史数据的汇总分析提示词
3. **历史汇总分析** - 停止采集时自动生成综合洞察报告

**重要更新**：分析与采集功能已合并为一个按钮，点击"开始分析与采集"会同时启动视频分析和历史记录采集，点击"停止分析并汇总"会停止分析并自动生成历史汇总报告。

## 功能详细说明

### 1. 统一的分析与采集控制

#### 开始分析与采集
- 点击 **"▶️ 开始分析与采集"** 按钮
- 系统会：
  1. 自动启动视频流
  2. 开始后端历史记录采集
  3. 开始定期的视频分析
  4. 在右上角显示 "🔴 正在采集历史记录..." 指示器

#### 停止分析并汇总
- 点击 **"⏹️ 停止分析并汇总"** 按钮
- 系统会：
  1. 停止视频采集和分析
  2. 停止记录新的分析历史
  3. 将所有采集的历史记录和汇总提示词一起提交给洞察模型
  4. 流式显示综合分析结果
  5. 停止视频流

### 2. 汇总提示词配置

在 **系统配置 → 📋 汇总提示词** 选项卡中：

- **配置汇总提示词**：自定义如何分析历史记录
  - 默认提示词：`请分析以下所有历史观察记录，提供综合洞察和总结：`
  - 可以根据需求修改，例如：
    - 趋势分析：`请分析以下历史记录，总结主要变化趋势和发展方向：`
    - 异常检测：`请检查以下历史记录，识别任何异常或不寻常的情况：`
    - 安全监控：`请评估以下历史记录中的安全状况和潜在风险：`

- **保存配置**：点击 **"💾 保存汇总提示词"** 按钮保存设置

### 3. 历史汇总分析显示

停止采集后，会在右侧显示 **"📊 历史汇总分析"** 卡片，包含：

- **采集条目数**：显示本次采集的分析记录数量
- **生成时间**：汇总分析的生成时间
- **汇总结果**：流式显示综合分析内容

## 使用流程

### 典型工作流程

1. **配置汇总提示词**（可选，在启动前配置）
   - 进入 "系统配置 → 📋 汇总提示词"
   - 根据需求修改提示词
   - 保存配置

2. **启动分析与采集**
   - 选择摄像头设备
   - 点击 "▶️ 开始分析与采集"
   - 系统自动启动视频流、分析和历史记录采集

3. **观察分析过程**
   - 查看实时分析结果
   - 右上角显示采集状态指示器
   - 历史记录自动保存

4. **停止并生成汇总**
   - 采集一段时间后，点击 "⏹️ 停止分析并汇总"
   - 系统停止分析并自动生成综合汇总报告
   - 等待汇总分析完成

5. **查看汇总结果**
   - 在 "历史汇总分析" 卡片中查看结果
   - 显示采集条目数和生成时间
   - 可以滚动查看完整内容

## API 端点说明

### 后端新增 API

#### 1. 开始采集
```
POST /collection/start
```
**响应**:
```json
{
  "status": "started",
  "historyCount": 0
}
```

#### 2. 停止采集并汇总
```
POST /collection/stop
Content-Type: application/json

{
  "summaryPrompt": "自定义汇总提示词",
  "apiUrl": "洞察模型API地址",
  "modelName": "洞察模型名称"
}
```
**响应**: SSE流式响应
- 首先返回元数据: `{"type":"metadata","historyCount":10,"status":"stopped"}`
- 然后流式返回汇总分析内容

#### 3. 获取采集状态
```
GET /collection/status
```
**响应**:
```json
{
  "isCollecting": true,
  "historyCount": 15,
  "summaryPrompt": "当前汇总提示词"
}
```

#### 4. 更新汇总提示词配置
```
POST /collection/config
Content-Type: application/json

{
  "summaryPrompt": "新的汇总提示词"
}
```

## 环境变量配置

可以通过环境变量设置默认汇总提示词：

```bash
export SUMMARY_PROMPT="请分析以下所有历史观察记录，提供综合洞察和总结："
```

## 技术实现要点

### 后端实现 (server/server.js)

1. **历史记录收集**
   - 在 `/analyze` 端点中解析 SSE 响应流
   - 当 `isCollecting=true` 时保存完整响应到 `analysisHistory` 数组
   - 每条记录包含：时间戳、提示词、响应内容

2. **汇总分析生成**
   - 将所有历史记录格式化为文本
   - 与汇总提示词组合
   - 调用洞察模型 API 进行综合分析
   - 流式返回结果

### 前端实现 (public/app.js & index.html)

1. **采集控制UI**
   - 两个按钮：开始采集、停止并汇总
   - 采集指示器显示当前状态
   - 汇总结果卡片动态显示

2. **配置界面**
   - 新增 "汇总提示词" 配置选项卡
   - 提示词输入框和保存按钮

3. **流式结果显示**
   - 解析 SSE 流式响应
   - 实时更新汇总结果显示
   - 自动滚动到最新内容

## 注意事项

1. **内存管理**：历史记录保存在服务器内存中，重启服务器会清空历史
2. **并发控制**：同时只能有一个采集会话
3. **提示词配置**：当前通过环境变量配置，重启服务器后恢复默认值
4. **网络超时**：汇总分析可能需要较长时间，已设置120秒超时

## 未来改进方向

1. **持久化存储**：将历史记录保存到数据库
2. **多会话支持**：支持多个用户同时采集
3. **配置持久化**：将汇总提示词配置保存到配置文件
4. **导出功能**：支持导出历史记录和汇总报告
5. **统计分析**：提供采集统计和可视化图表

## 示例场景

### 场景1：安全监控
1. 配置汇总提示词：`请分析以下监控记录，识别安全隐患和异常行为：`
2. 开始采集，持续监控30分钟
3. 停止并汇总，获取安全评估报告

### 场景2：活动记录
1. 配置汇总提示词：`请总结以下时间段内的主要活动和事件：`
2. 开始采集，记录完整活动过程
3. 停止并汇总，获取活动记录摘要

### 场景3：趋势分析
1. 配置汇总提示词：`请分析以下观察记录，识别变化趋势和规律：`
2. 开始采集，记录一段时间的变化
3. 停止并汇总，获取趋势分析报告
