# 视觉分析监控系统 - 新功能说明

## 新增功能：独立视频流控制

### 功能概述

系统现在将视频流控制和AI分析功能完全分离，提供更灵活的操作体验。

### 主要改进

#### 1. UVC设备选择
- 📹 在视频预览区域添加设备选择下拉框
- 支持在启动前选择不同的UVC摄像头
- 默认使用系统默认摄像头设备

#### 2. 独立的视频流控制
- **🎬 启动视频流**按钮：连接并启动UVC摄像头，开始实时推送视频帧
- **⏸️ 停止视频流**按钮：停止摄像头采集，完全断开视频连接
- 视频流可以独立运行，无需启动AI分析

#### 3. 独立的分析控制
- **▶️ 开始分析**按钮：在视频流基础上启动AI分析（必须先启动视频流）
- **⏹️ 停止分析**按钮：停止AI分析，但保持视频流继续运行
- 可以随时开始/停止分析而不影响视频流

### 使用流程

```
步骤 1: 选择UVC设备（可选）
   ↓
步骤 2: 点击"启动视频流" → 视频预览开始显示
   ↓
步骤 3: 点击"开始分析" → AI开始分析视频内容
   ↓
步骤 4: 随时可以"停止分析"（视频流继续）
   ↓
步骤 5: 需要时"停止视频流"（完全关闭摄像头）
```

### 界面说明

**视频预览卡片**包含：
1. UVC设备下拉选择框
2. 视频流控制按钮组（启动视频流 / 停止视频流）
3. 分析控制按钮组（开始分析 / 停止分析）
4. 状态指示器（显示当前系统状态）

### 状态说明

- **未启动视频流**：初始状态，需要启动视频流
- **视频流已启动**：摄像头正在工作，可以开始分析
- **视频流运行中**：视频流正常，但未启动分析
- **正在采集**：正在采集帧并进行AI分析
- **已缓存 X/Y 帧**：显示当前缓存的帧数
- **视频流已停止**：摄像头已关闭

### 技术实现

**前端（app.js）**
- 添加视频流控制函数：`startVideoStream()` 和 `stopVideoStream()`
- 添加分析控制函数：`startAnalysis()` 和 `stopAnalysis()`
- 通过WebSocket发送控制命令（start_stream / stop_stream）
- 状态管理：`isStreamActive` 和 `isAnalyzing`

**后端（server.js）**
- WebSocket消息处理：监听start_stream和stop_stream命令
- 摄像头控制函数：`startCameraStream()` 和 `stopCameraStream()`
- 支持设备切换（通过device参数）
- 避免重复启动（检查captureInterval状态）

### 启动方法

**推荐使用启动脚本：**
```bash
cd /Users/thomas/rm01/vlmTest/server
./start.sh
```

**或手动启动：**
```bash
cd /Users/thomas/rm01/vlmTest/server
PATH="/opt/homebrew/bin:$PATH" node server.js
```

然后在浏览器中打开：`http://localhost:3000`

### 优势

1. ✅ **更灵活的控制**：可以只开视频流而不分析，节省资源
2. ✅ **设备选择**：支持多摄像头环境下的设备切换
3. ✅ **清晰的状态**：按钮状态和文本清楚地反映系统状态
4. ✅ **独立操作**：视频流和分析功能互不干扰
5. ✅ **资源优化**：不需要分析时可以停止AI处理

### 注意事项

- 必须先启动视频流，才能开始分析
- 停止分析不会停止视频流
- 停止视频流会自动停止分析（如果正在分析）
- 设备选择需要在启动视频流前完成
- 视频流运行时设备选择框会被禁用

### 常见问题

**Q: 点击"开始分析"没反应？**
A: 请先点击"启动视频流"按钮，等待视频预览出现后再开始分析。

**Q: 如何更换摄像头设备？**
A: 先停止视频流，在设备下拉框中选择其他设备，然后重新启动视频流。

**Q: 如何只预览视频不分析？**
A: 只需点击"启动视频流"，不要点击"开始分析"即可。

**Q: 停止分析后视频还在运行？**
A: 是的，这是设计行为。停止分析不会影响视频流，如需完全关闭请点击"停止视频流"。

### 相关文件

- `/public/index.html` - 添加了设备选择框和新的控制按钮
- `/public/app.js` - 实现了独立的视频流和分析控制逻辑
- `/server/server.js` - 添加了WebSocket命令处理和视频流控制
- `/server/start.sh` - 启动脚本，自动配置PATH环境变量
